import { TallerPageContent } from '@/components/taller/taller-page-content';

export const metadata = {
  title: 'Taller',
};

export default function TallerPage() {
  return <TallerPageContent />;
}
